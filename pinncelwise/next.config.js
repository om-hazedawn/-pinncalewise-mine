/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    turbo: {
      loaders: {
        // Configure other loaders here
      },
      rules: {
        // Configure other rules here
      }
    }
  }
}

module.exports = nextConfig
